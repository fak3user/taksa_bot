package bot

func AddToChat(chatId int64) bool {
	/*
	* @Todo:
	*
	* Реализовать запись в базу,
	* обработчики на success и error от базы
	* написаны ниже
	*
	 */

	if true { // onSuccess
		return true
	} else { // onError
		return false
	}
}

func RemoveFromChat(chatId int64) bool {
	/*
	* @Todo:
	*
	* Реализовать запись в базу,
	* обработчики на success и error от базы
	* написаны ниже
	*
	 */

	if true { // onSuccess
		return true
	} else { // onError
		return false
	}
}
